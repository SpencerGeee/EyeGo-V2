/**
 * The one landing spot every "open the search sheet" morph shares — the home
 * where-to pill and the three service cards all grow into the same card, so
 * they share one prediction memory. See `MorphToOptions.landingKey`.
 */
export const TRIP_SEARCH_LANDING = 'trip-search-card';
